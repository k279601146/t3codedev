# T3 Code Desktop 打包问题总结与正确打包指南

## 一、打包失败的原因

### 1. 文件编码损坏（UTF-8 乱码）

**问题**：`apps/desktop/src/app/DesktopEnvironment.ts` 中的中文注释在某次编辑后出现乱码（`�?` 符号），导致 TypeScript 编译失败或运行时行为异常。

**表现**：

```
// 原本正确的注释：
/** 捆绑 AI 引擎二进制的绝对路径（可能不存在） */

// 损坏后变成：
/** 捆绑 AI 引擎二进制的绝对路径（可能不存在�?*/
```

**原因**：编辑器或 Git 操作中文件编码被错误转换（可能保存时使用了非 UTF-8 编码）。

**修复**：commit `f87c6b3f` 手动恢复了正确的 UTF-8 中文注释。

---

### 2. 环境变量/路径名统一（从 `T3CODE_HOME` 迁移至 `BAHEW_HOME`，`.t3` 迁移至 `.bahew`）

**说明**：项目已全面统一使用 `BAHEW_HOME` 作为主环境变量，默认基目录对齐为 `.bahew`。这一改动是为了品牌重塑及确保全链路路径一致。

| 文件                                    | 错误改动                       |
| --------------------------------------- | ------------------------------ |
| `apps/desktop/src/app/DesktopConfig.ts` | `T3CODE_HOME` → `BAHEW_HOME`   |
| `packages/ssh/src/tunnel.ts`            | `$HOME/.t3/` → `$HOME/.bahew/` |

**后果**：打包后的应用找不到正确的配置目录和环境变量，启动失败。

**当前状态**：已完成。`DesktopConfig.ts`、`DesktopEnvironment.ts`、`dev-runner.ts` 及所有测试用例均已对齐使用 `BAHEW_HOME`。

---

### 3. Windows 平台 `bun install` 兼容性问题

**问题**：原始打包脚本在 staging 目录使用 `bun install --production --omit optional`，在 Windows 环境下 bun 命令行为不稳定。

**修复**（commit `dc9b00ff`）：Windows 平台改用 `npm install`：

```typescript
const installCommand =
  process.platform === "win32"
    ? "npm install --production --omit=optional --ignore-scripts"
    : "bun install --production --omit optional";
```

---

### 4. `engine-bin` 打包资源缺失

**问题**：AI 引擎二进制文件（`ai-engine.exe`、`command-runner.exe`）没有被正确打包到安装包中。

**修复**（commit `dc9b00ff`）：

- 在 `extraResources` 配置中添加 `engine-bin` 目录
- 在打包流程中将 `apps/desktop/bin/` 下的文件复制到 staging 的 `engine-bin/` 目录

---

## 二、当前遗留问题

以下问题仍存在于代码中，打包前需确认或修复：

1. **确保 `BAHEW_HOME` 在所有配置中生效**
2. **确保 SSH tunnel 脚本及桌面端默认基目录对齐为 `.bahew`**
3. **DEBUG 日志需要移除**（`apps/server/src/provider/Layers/CodexSessionRuntime.ts:869-872`）—— 这些是调试用临时代码，不应进入正式打包

---

## 三、正确打包步骤

### 前置准备

1. 确保 Node.js v24+ 和 bun v1.3.11+ 已安装
2. 确保 `apps/desktop/bin/` 下有正确的引擎二进制文件：
   - `ai-engine.exe`（Windows）
   - `command-runner.exe`（Windows）

### 打包命令

```powershell
# Windows NSIS 安装包（自动检测架构）
bun run dist:desktop:win

# 指定架构
bun run dist:desktop:win:x64
bun run dist:desktop:win:arm64

# macOS DMG
bun run dist:desktop:dmg
bun run dist:desktop:dmg:arm64
bun run dist:desktop:dmg:x64

# Linux AppImage
bun run dist:desktop:linux
```

### 打包前检查清单

- [x] `apps/desktop/src/app/DesktopConfig.ts` 中环境变量名正确（`BAHEW_HOME`）
- [x] `packages/ssh/src/tunnel.ts` 中路径正确（`$HOME/.bahew/`）
- [ ] `apps/desktop/bin/` 下有对应平台的引擎二进制文件
- [ ] 移除所有 `[DEBUG]` 临时日志
- [ ] 源文件无编码乱码（特别是中文注释）
- [ ] 运行 `bun run build:desktop` 确认编译通过
- [ ] 运行 `bun run typecheck` 确认类型检查通过

### 打包流程说明

`dist:desktop:win` 实际调用 `scripts/build-desktop-artifact.ts`，流程如下：

1. 编译 desktop 和 server 代码（`turbo build`）
2. 创建 staging 目录，复制编译产物
3. 复制 `apps/desktop/bin/` 引擎文件到 `engine-bin/`
4. 复制 resource 文件（图标等）
5. 生成 staging `package.json`，安装生产依赖
6. 调用 electron-builder 打包为安装程序

---

## 四、注意事项

1. **编码问题防范**：编辑器请始终使用 UTF-8 (without BOM) 保存 `.ts` 文件，避免使用会自动转换编码的工具
2. **全局替换要谨慎**：不要对 `t3`、`T3CODE` 等关键标识符做全局替换，这些是应用核心路径
3. **打包前先 build**：不要跳过 `bun run build:desktop`，直接运行 `dist:desktop:*` 已包含 build 步骤，但提前验证能更快发现问题
4. **Windows 环境特殊性**：Windows 打包使用 `npm install` 而非 `bun install`，确保 npm 可用
